[project]
name = "gig-finder"
version = "0.1.0"
description = "Claude skill that finds concerts matching your music taste."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
    "python-dateutil>=2.9",
    "rapidfuzz>=3.9",
]

[tool.uv]
package = false
